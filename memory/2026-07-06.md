# Memory 2026-07-06

## PROJ-20: Logistik & Abholung (In Progress → Deployment)

### DB Migration (erledigt)
- ✅ `tms.blocked_days` Tabelle erstellt (Blocker-Tage / Feiertage NRW)
- ✅ Status-Enum umbenannt: `abgeholt` → `erledigt`
- ✅ `pickup_day` Spalte zur `tms.partner_order_defaults` hinzugefügt
- ✅ 545 bestehende Kunden mit Abholservice → `pickup_day = 1` (Montag) gesetzt

### Bugs gefunden & gefixt
1. **Status-Wert "geplan" → "geplant"**: Mein Code verwendete `geplan`, aber die DB hatte `geplant` (mit t). ENUM wurde korrigiert.
2. **Spaltennamen falsch**: 
   - Code: `pickup_cycle_weeks` → DB: `pickup_cycle_count`
   - Code: `pickup_status` → DB: `pickup_delivery_status`
   - Code: `planned_date` → DB: `geplantes_abholdatum`
   - Fix: Actions an DB-Spaltennamen angepasst

3. **@supabase/ssr Client + Schema = doppeltes Schema**: 
   - `@supabase/ssr` mit `.schema("tms")` erzeugt URL `/rest/v1/tms.tours` → Supabase interpretiert als `tms.tms.tours` (❌)
   - `supabase-js` mit `db: { schema: "tms" }` sendet `Accept-Profile: tms` Header → korrekt (✅)
   - Fix: `getNextPickupTour` auf `createAdminClient` (supabase-js) umgestellt

### Deployment
- Docker-Container `tms` läuft auf Hetzner-Server
- Next.js standalone build in Docker
- Traefik reverse proxy routed zu Container
- Docker-Image neu gebaut und Container gestartet
- Build: 0 Fehler

### Offen / Zu testen
- NextPickupCard zeigt trotz fix evtl. noch keine Tour-Daten → Lade-Test vom User erwartet
- `/verwaltung/blocker` Seite noch nicht getestet
- Automatische Abholungserstellung nach "erledigt"-Status noch nicht getestet

### Technische Erkenntnis
- `@supabase/ssr` createServerClient mit `.schema()` verwendet URL-basiertes Schema → funktioniert NICHT mit self-hosted Supabase wenn Schema nicht public ist
- `@supabase/supabase-js` createClient mit `db: { schema: "tms" }` verwendet `Accept-Profile` Header → funktioniert korrekt
- Für Schema != "public" immer supabase-js Client mit Header-Schema verwenden

## User-Request: "Matthias Büsken" prüfen
- Kunde hat Abholservice + 2-Wochen-Rhythmus + Automatisch
- Tour existiert: Status "geplant", Datum 2026-07-07
- pickup_day war NULL → auf 1 (Montag) gesetzt
