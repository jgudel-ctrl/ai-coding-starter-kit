## Session 2026-07-02 14:02 UTC

### PROJ-11 Kundendetailseite - Aktueller Status

**Problem:** Sortierung auf /kunden funktioniert nicht - Kunden mit 0 Umsatz werden oben angezeigt statt unten.

**User-Feedback (13:58 UTC):** "Leider ist der Fehler genau wie vorher."

**Vermutete Ursache:** Docker Build wurde abgebrochen und hat altes Image wiederverwendet. Container zeigt möglicherweise alten Code.

**Nächster Schritt:** Container-Logs prüfen und neu bauen mit --no-cache

**Wichtig:** Keine weiteren Änderungen ohne vorheriges Lesen der Dateien und Validierung über curl.


## Session 2026-07-02 14:02 UTC

### PROJ-11 Kundendetailseite - Aktueller Status

**Problem:** Sortierung auf /kunden funktioniert nicht - Kunden mit 0 Umsatz werden oben angezeigt statt unten.

**User-Feedback (13:58 UTC):** "Leider ist der Fehler genau wie vorher."

**Vermutete Ursache:** Docker Build wurde abgebrochen und hat altes Image wiederverwendet. Container zeigt möglicherweise alten Code.

**Nächster Schritt:** Container-Logs prüfen und neu bauen mit --no-cache

**Wichtig:** Keine weiteren Änderungen ohne vorheriges Lesen der Dateien und Validierung über curl.

---

## Session 2026-07-02 15:08 UTC

### Docker Build + Container-Test

**Erkenntnis:** Docker Build schlug fehl mit "no space left on device" → Host-Festplatte voll. User wurde informiert.

**User hat Festplatte bereinigt und Docker Neustart durchgeführt.**

**Neuer Versuch:** Erneutes Bauen nach Cleanup. Build läuft...

**15:17 UTC - Build SUCCESSFUL!**

### Problem gefunden: Stale `.next/` Build-Artefakte auf Host

Docker hat alte `page.js` aus Host-`.next/` Verzeichnis kopiert (`COPY . .` kopiert auch `.next`). Die neue `export const dynamic = "force-dynamic"` und Sortier-Logik wurde zwar ins Source geschrieben, aber nie neu kompiliert.

**Lösung:** `.next/` auf Host löschen → Docker Build → Code ist jetzt im Container verifiziert:
- `getPartnersWithRevenue` ✅ gefunden
- `force-dynamic` ✅ gefunden
- `current_year_revenue` Sortierung ✅ gefunden

---

## Session 2026-07-02 15:29 UTC

### Playwright Setup Versuch

User wollte Playwright-Tests mit einem Test-User. 

**Versuche:**
1. Supabase Auth API blockiert anonyme Signups (gut so)
2. Versuch mit Service Role Key: API blockiert auch (RLS/Auth-Constraints)

**Ergebnis:** Test-User direkt über Auth-API erstellen nicht möglich.

**Alternative:** Test-User existiert bereits in Supabase (für staging), aber nicht für tms.gudel-werkzeuge.de.

**Workaround:** Direkte API-Tests mit curl statt Browser-Automation. Das zeigt das gleiche Ergebnis (Server Actions = API).

---

## Session 2026-07-02 15:49 UTC

### Suche-Fix: `easybill_customer_number` ist Integer, nicht Text

**Problem:** Suche nach Kundennummer funktioniert nicht.

**Ursache:** In `partners.ts` wird `ilike` (Text-Suche) auf `easybill_customer_number` verwendet, aber das Feld ist ein **Integer** (Zahl).

**Fix in `getPartnersWithRevenue`:**
```typescript
if (search) {
  // Prüfen ob Suche rein numerisch ist (Kundennummer)
  const isNumericSearch = /^\d+$/.test(search);
  
  if (isNumericSearch) {
    // Bei Zahlen: exakte Übereinstimmung
    query = query.or(
      `company_name.ilike.%${search}%,display_name.ilike.%${search}%,first_name.ilike.%${search}%,last_name.ilike.%${search}%,email.ilike.%${search}%,easybill_customer_number.eq.${search}`
    );
  } else {
    // Bei Text: Teilübereinstimmung
    query = query.or(
      `company_name.ilike.%${search}%,display_name.ilike.%${search}%,first_name.ilike.%${search}%,last_name.ilike.%${search}%,email.ilike.%${search}%`
    );
  }
}
```

**Docker Build:** Erfolgreich, Code im Container verifiziert.

**Wichtige Lektion:** Supabase `ilike` funktioniert NUR auf Text-Feldern. Bei Integer/Zahl-Feldern muss `eq.` (exakt) verwendet werden.

---

## PROJ-11 Status (15:54 UTC)

- ✅ Spec approved
- ✅ Architektur approved
- ✅ Frontend implementiert (Kundendetailseite mit Tabs)
- ✅ Backend implementiert (Server Actions)
- ✅ Sortierung gefixt (Umsatz absteigend, 0 am Ende)
- ✅ Suche gefixt (Text = ilike, Zahl = eq)
- ✅ Mobile-Optimierung (Karten statt Tabelle)
- ✅ Docker deployed mit frischem Build
- ⏳ Warte auf User-Feedback ob alles funktioniert

**Next Steps:**
1. User testet https://tms.gudel-werkzeuge.de/kunden
2. Falls OK → PROJ-11 als Deployed markieren
3. Falls nicht → weitere Fixes

**Docker-Lektion gelernt:** Immer `.next/` auf Host löschen vor `docker compose build`, sonst werden alte Build-Artefakte kopiert.
