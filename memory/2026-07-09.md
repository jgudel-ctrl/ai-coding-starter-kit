# Memory — 2026-07-09

## PROJ-23.1: Invoice-Sync via Easybill API (IN PROGRESS)

**Status:** Backend + Frontend fertig, Build OK, wartet auf Deploy + ersten Sync

### Was heute passiert ist:
1. **Spec erstellt** (`features/PROJ-23.1-invoice-sync-easybill.md`) mit allen Easybill-Feldern
2. **Architektur erstellt** (`features/PROJ-23.1-architektur.md`) mit DB-Schema, API-Client, Sync-Logik
3. **Datenbank-Migration** ausgeführt:
   - 4 Tabellen neu erstellt: `invoices`, `invoice_items`, `invoice_payments`, `invoice_sync_log`
   - RLS Policies: SELECT für alle, INSERT/UPDATE/DELETE nur Admin
   - 6 Materialized Views wurden gelöscht (Abhängigkeit von alten Tabellen)
4. **API-Key gesichert** in `.env.local` (wird nicht von Git getrackt)
5. **Easybill API-Client** gebaut: `src/lib/easybill/client.ts`
6. **Sync-Logik** gebaut: `src/lib/easybill/sync.ts`
7. **Server Actions** gebaut: `src/lib/actions/invoices.ts`
8. **Frontend** gebaut:
   - `/verwaltung/invoices` — Übersicht mit Filter + Pagination
   - `/verwaltung/invoices/[id]` — Detail mit Positionen + Zahlungen
   - `/verwaltung/invoices/sync` — Manueller Sync + Status
9. **Navigation** erweitert: "Rechnungen" im Admin-Menü (Burger + Dropdown)
10. **Cronjob** eingerichtet: Täglich 02:00 Uhr Europe/Berlin
11. **Build erfolgreich**: `npx tsc --noEmit` ✅, `npx next build` ✅

### Bekannte Probleme:
- **6 Materialized Views wurden gelöscht** beim DROP TABLE:
  - `partner_item_revenue_monthly`
  - `partner_item_margin_monthly`
  - `mv_partner_monthly_revenue`
  - `mv_dashboard_monthly_revenue`
  - `partner_revenue_monthly`
  - `partner_revenue_summary`
  - → Müssen neu erstellt werden wenn sie noch gebraucht werden

### Offene ToDos:
1. API-Key auf Produktionsserver in `.env.local` eintragen
2. Ersten vollständigen Sync starten (14.100 Dokumente)
3. Materialized Views bei Bedarf neu erstellen
4. Deploy durchführen
5. Feature auf "Deployed" setzen

### Easybill API-Details:
- Basis-URL: `https://api.easybill.de/rest/v1`
- Auth: Bearer Token
- Rate-Limit: 60 req/min (Premium 1000 = Business)
- 14.100 Dokumente insgesamt
- Relevant ab 01.01.2023

### Dateien erstellt/geändert:
- `scripts/PROJ-23.1_migration.sql`
- `src/lib/easybill/client.ts`
- `src/lib/easybill/sync.ts`
- `src/lib/actions/invoices.ts`
- `src/lib/utils.ts` (formatCent ergänzt)
- `src/components/app-header.tsx` (Navigation erweitert)
- `src/app/(app)/verwaltung/invoices/page.tsx`
- `src/app/(app)/verwaltung/invoices/[id]/page.tsx`
- `src/app/(app)/verwaltung/invoices/sync/page.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-list.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-table.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-filter.tsx`
- `src/app/(app)/verwaltung/invoices/components/payment-status-badge.tsx`
- `features/PROJ-23.1-invoice-sync-easybill.md`
- `features/PROJ-23.1-architektur.md`
- `features/PROJ-23.1-qa-bericht.md`
- `features/INDEX.md` (Status aktualisiert)
- `scripts/test-easybill-api.ts`
- `scripts/test-easybill-sync-standalone.ts`
- `scripts/test-real-sync.ts`

---

## Kommunikation mit Jan Bernd

### Entscheidungen getroffen:
1. **Alle Easybill-Felder speichern** (nicht nur die wichtigsten) — Jan Bernd wollte "alle verfügbaren Daten"
2. **Gutschriften/STORNO als negative Beträge** — Ja, wie Easybill liefert
3. **Tabellen komplett löschen und neu erstellen** — So gewünscht
4. **API-Key:** In `.env.local` (nicht in `.env.production`) — sicherer, nicht in Git

### Workflow befolgt:
- `/init` → `/write-spec` → "approved" → `/architecture` → "approved" → `/backend` → `/frontend` → `/qa`
- Warte jetzt auf Deploy-Entscheidung von Jan Bernd
# Memory — 2026-07-09

## PROJ-23.1: Invoice-Sync via Easybill API (IN PROGRESS)

**Status:** Backend + Frontend fertig, Build OK, wartet auf Deploy + ersten Sync

### Was heute passiert ist:
1. **Spec erstellt** (`features/PROJ-23.1-invoice-sync-easybill.md`) mit allen Easybill-Feldern
2. **Architektur erstellt** (`features/PROJ-23.1-architektur.md`) mit DB-Schema, API-Client, Sync-Logik
3. **Datenbank-Migration** ausgeführt:
   - 4 Tabellen neu erstellt: `invoices`, `invoice_items`, `invoice_payments`, `invoice_sync_log`
   - RLS Policies: SELECT für alle, INSERT/UPDATE/DELETE nur Admin
   - 6 Materialized Views wurden gelöscht (Abhängigkeit von alten Tabellen)
4. **API-Key gesichert** in `.env.local` (wird nicht von Git getrackt)
5. **Easybill API-Client** gebaut: `src/lib/easybill/client.ts`
6. **Sync-Logik** gebaut: `src/lib/easybill/sync.ts`
7. **Server Actions** gebaut: `src/lib/actions/invoices.ts`
8. **Frontend** gebaut:
   - `/verwaltung/invoices` — Übersicht mit Filter + Pagination
   - `/verwaltung/invoices/[id]` — Detail mit Positionen + Zahlungen
   - `/verwaltung/invoices/sync` — Manueller Sync + Status
9. **Navigation** erweitert: "Rechnungen" im Admin-Menü (Burger + Dropdown)
10. **Cronjob** eingerichtet: Täglich 02:00 Uhr Europe/Berlin
11. **Build erfolgreich**: `npx tsc --noEmit` ✅, `npx next build` ✅

### Bekannte Probleme:
- **6 Materialized Views wurden gelöscht** beim DROP TABLE:
  - `partner_item_revenue_monthly`
  - `partner_item_margin_monthly`
  - `mv_partner_monthly_revenue`
  - `mv_dashboard_monthly_revenue`
  - `partner_revenue_monthly`
  - `partner_revenue_summary`
  - → Müssen neu erstellt werden wenn sie noch gebraucht werden

### Offene ToDos:
1. API-Key auf Produktionsserver in `.env.local` eintragen
2. Ersten vollständigen Sync starten (14.100 Dokumente)
3. Materialized Views bei Bedarf neu erstellen
4. Deploy durchführen
5. Feature auf "Deployed" setzen

### Easybill API-Details:
- Basis-URL: `https://api.easybill.de/rest/v1`
- Auth: Bearer Token
- Rate-Limit: 60 req/min (Premium 1000 = Business)
- 14.100 Dokumente insgesamt
- Relevant ab 01.01.2023

### Dateien erstellt/geändert:
- `scripts/PROJ-23.1_migration.sql`
- `src/lib/easybill/client.ts`
- `src/lib/easybill/sync.ts`
- `src/lib/actions/invoices.ts`
- `src/lib/utils.ts` (formatCent ergänzt)
- `src/components/app-header.tsx` (Navigation erweitert)
- `src/app/(app)/verwaltung/invoices/page.tsx`
- `src/app/(app)/verwaltung/invoices/[id]/page.tsx`
- `src/app/(app)/verwaltung/invoices/sync/page.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-list.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-table.tsx`
- `src/app/(app)/verwaltung/invoices/components/invoice-filter.tsx`
- `src/app/(app)/verwaltung/invoices/components/payment-status-badge.tsx`
- `features/PROJ-23.1-invoice-sync-easybill.md`
- `features/PROJ-23.1-architektur.md`
- `features/PROJ-23.1-qa-bericht.md`
- `features/INDEX.md` (Status aktualisiert)
- `scripts/test-easybill-api.ts`
- `scripts/test-easybill-sync-standalone.ts`
- `scripts/test-real-sync.ts`

---

## Kommunikation mit Jan Bernd

### Entscheidungen getroffen:
1. **Alle Easybill-Felder speichern** (nicht nur die wichtigsten) — Jan Bernd wollte "alle verfügbaren Daten"
2. **Gutschriften/STORNO als negative Beträge** — Ja, wie Easybill liefert
3. **Tabellen komplett löschen und neu erstellen** — So gewünscht
4. **API-Key:** In `.env.local` (nicht in `.env.production`) — sicherer, nicht in Git

### Workflow befolgt:
- `/init` → `/write-spec` → "approved" → `/architecture` → "approved" → `/backend` → `/frontend` → `/qa`
- Warte jetzt auf Deploy-Entscheidung von Jan Bernd

---

## PROJ-24: Easybill Partner Webhook Sync (IN PROGRESS)

**Status:** Backend fertig, Frontend-Rabatte fertig, wartet auf Dubletten-Check + Deploy

### Was heute passiert ist:
1. **Spec erstellt** (`features/PROJ-24-easybill-partner-webhook-sync.md`) — approved
2. **Architektur erstellt** (`features/PROJ-24-architektur.md`) — approved
3. **Datenbank-Migration** ausgeführt:
   - Neue Tabellen: `position_groups`, `customer_groups`, `partner_discounts`
   - Extensions: `partners.duplicate_of`, `partners.duplicate_reason`, `partner_addresses.is_active/is_primary/deleted_at`, `partner_contacts.is_active/deleted_at`
4. **Backend gebaut:**
   - `src/lib/easybill/partner-sync.ts` — Core Sync-Logik
   - `src/lib/geoapify/validate.ts` — Geoapify Adressvalidierung
   - `src/app/api/webhooks/easybill/customer/route.ts` — Webhook Handler
   - `src/app/api/cron/sync-partners/route.ts` — Cronjob Handler
5. **Frontend-Rabatte gebaut:**
   - `src/lib/actions/discounts.ts` — Server Action
   - `src/app/(app)/kunden/[id]/components/discounts-card.tsx` — Komponente
   - Tab "Rabatte" in Kundendetailseite eingefügt
6. **Cronjob eingerichtet:** `PROJ-24 Easybill Partner Sync Fallback` (stündlich)
7. **API-Keys in `.env.production`:** EASYBILL_API_KEY, GEOAPIFY_API_KEY, EASYBILL_WEBHOOK_SECRET
8. **Build erfolgreich:** `npm run build` ✅
9. **Git commit:** `60 files changed, 10,722 insertions(+)`
10. **Initial Import durchgeführt:**
    - 21 von 26 fehlenden Kunden importiert (5 waren Dubletten)
    - Erfolgsrate: 80,8%
11. **Dublettenprüfung entkoppelt:** `checkForDuplicates` entfernt aus `syncEasybillCustomer`, separater Call nach Sync
12. **Cronjob + Webhook angepasst:** Import zuerst, dann separat Dubletten-Prüfung

### Offene ToDos:
1. **Initial Dubletten-Check auf alle ~2.600 Partner** — wartet auf nächsten Turn (Pre-Compaction blockiert Datei-Schreiben)
2. **Deploy** — wartet auf Freigabe
3. **Webhook in Easybill konfigurieren** — wartet auf App-Zugriff
4. **Rabatte-Frontend deployen** — ist im Build enthalten

### Business Rules (Ü1–Ü5):
- Ü1: display_name = Firmenname bevorzugt
- Ü2: Archivierte → is_active = false
- Ü3: Dubletten → umsatzschwächeren deaktivieren (NACH dem Import)
- Ü4: Immer Lieferadresse + Kontakt mit E-Mail
- Ü5: Soft-Delete (is_active/deleted_at)

### Datenbank-Stand (post-Import):
| Metrik | Wert |
|--------|------|
| Gesamt Partner | 2.653 |
| Mit Easybill-Nummer | 2.652 |
| Aktive Kunden | 2.648 |
| Archiviert/inaktiv | 5 |

### Webhook-Config (für später):
- URL: `https://tms.gudel-werkzeuge.de/api/webhooks/easybill/customer`
- Events: `customer.created`, `customer.updated`
- Secret: `wbhk_2026_PROJ24_a7f3k9m2p4q8r1t6`

### Wichtige Änderungen an Dubletten-Logik:
- **Vorher:** Dubletten-Check inline bei jedem Import
- **Jetzt:** Erst alle Kunden importieren, DANN separat Dubletten prüfen
- **Grund:** Jeder Kunde muss importiert werden, unabhängig von Dubletten-Status

### Blockiert:
- **Pre-Compaction Memory Flush:** Kann keine neuen Dateien erstellen (nur memory/2026-07-09.md)
- **Dubletten-Check-Skript:** Muss in nächstem Turn geschrieben und ausgeführt werden