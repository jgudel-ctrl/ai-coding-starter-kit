# 2026-07-10 — Freitag

## Nachtlicher Heartbeat (02:01 UTC)
- Memory-Check durchgeführt
- Keine neuen Features/Tickets seit gestern
- Projekt-Stand: PROJ-24 (Dubletten-Prüfung) ist deployed, Frontend zeigt Bezahlt-Status grün
- Git-Commit der Heartbeat-Updates lokal erfolgreich, Push fehlgeschlagen (kein Upstream — kein Problem)

## Morgendlicher Heartbeat (07:02 UTC)
- Git-Status: 1 uncommitted Datei (`kanban` — nur Zeilenumbruch, keine substanziellen Änderungen)
- Letzter Commit: Kundensuche mit Toggle für aktive/inaktive Kunden + Dubletten-Check initial
- Features/INDEX.md: Alle Features bis PROJ-24 deployed ✅
- Kanban (`kanban/tasks.json`) ist veraltet — zeigt noch alte PROJ-IDs (PROJ-1 bis PROJ-10). Sollte bei Gelegenheit aktualisiert oder als archiviert markiert werden.
- Keine neuen Telegram-Nachrichten oder Features seit letztem Check
- Keine dringenden Aktionen nötig

## Heartbeat (10:05 UTC)
- Memory-Index defekt → Neubau gestartet
- Kanban-Submodul uncommitted (dirty), nur Whitespace — nicht kritisch
- Keine neuen Features/Telegram-Nachrichten
- Keine dringenden Aktionen nötig
# 2026-07-10 — Freitag

## Nachtlicher Heartbeat (02:01 UTC)
- Memory-Check durchgeführt
- Keine neuen Features/Tickets seit gestern
- Projekt-Stand: PROJ-24 (Dubletten-Prüfung) ist deployed, Frontend zeigt Bezahlt-Status grün
- Git-Commit der Heartbeat-Updates lokal erfolgreich, Push fehlgeschlagen (kein Upstream — kein Problem)

## Morgendlicher Heartbeat (07:02 UTC)
- Git-Status: 1 uncommitted Datei (`kanban` — nur Zeilenumbruch, keine substanziellen Änderungen)
- Letzter Commit: Kundensuche mit Toggle für aktive/inaktive Kunden + Dubletten-Check initial
- Features/INDEX.md: Alle Features bis PROJ-24 deployed ✅
- Kanban (`kanban/tasks.json`) ist veraltet — zeigt noch alte PROJ-IDs (PROJ-1 bis PROJ-10). Sollte bei Gelegenheit aktualisiert oder als archiviert markiert werden.
- Keine neuen Telegram-Nachrichten oder Features seit letztem Check
- Keine dringenden Aktionen nötig

## Heartbeat (10:05 UTC)
- Memory-Index defekt → Neubau gestartet
- Kanban-Submodul uncommitted (dirty), nur Whitespace — nicht kritisch
- Keine neuen Features/Telegram-Nachrichten
- Keine dringenden Aktionen nötig

## Entwicklungssession: PROJ-24 Easybill Partner Sync (09:00–10:30 UTC)

Große Sessions heute. Jan Bernd hat "Mit 24 weitermachen" gesagt — und das haben wir.

### Erledigt:

1. **Easybill Webhook** — Konfiguriert (ID: 37919) für `customer.create` + `customer.update`
   - App Proxy fix: `/api/webhooks/*` von Auth ausgeschlossen
   - Sync-Funktion: Schema `tms` statt `default`
   - Webhook-URL: `https://tms.gudel-werkzeuge.de/api/webhooks/easybill/customer`

2. **Dubletten-Check** — 44 Duplikate gefunden und deaktiviert (aus 2.648 aktiven)
   - Normalisierte Namen (lowercase, special chars entfernt) + Adressvergleich
   - Lower-Revenue-Partner wird deaktiviert
   - Nur via "Alle"-Toggle in Kundensuche sichtbar

3. **Kundensuche erweitert** — Toggle für aktive/inaktive Kunden
   - Default: nur aktive
   - "Alle" zeigt auch deaktivierte Dubletten

4. **Initialer Import** — 4 fehlende Easybill-Kunden importiert
   - Alle mit Warnung (fehlende E-Mail)

5. **Stündlicher Cronjob** — systemd-Timer (`easybill-sync.timer`)
   - Läuft jede Stunde zu :00
   - Fallback für Webhook-Ausfälle
   - Logs in `easybill_sync_logs`

6. **Geoapify Adressvalidierung**
   - DB-Migration: 7 neue Spalten in `partner_addresses`
   - Modul `src/lib/geoapify/validate-address.ts`
   - Integration in Webhook + Cronjob
   - Nach Adress-Update automatisch revalidieren

7. **Admin-UI Kundendetailseite**
   - 🟢🟡🔴 Icons neben Straße
   - Gelber Vorschlag-Kasten mit "Vergleich anzeigen"
   - Dialog: Links = Original, Rechts = Vorschlag
   - Button "Vorschlag übernehmen" → speichert + validiert neu

8. **Massen-Validierung** — `scripts/bulk-geoapify-validate.js`
   - 5.176 bestehende Adressen validiert
   - Rate-Limited: 200ms zwischen Anfragen (5/sec)
   - Ergebnis: 4.808 gültig (92.9%), 355 ungültig (6.9%), 13 Vorschläge (0.3%)

### Neue Dateien:
- `src/lib/geoapify/validate-address.ts`
- `scripts/cron-easybill-sync.js` + `.service` + `.timer`
- `scripts/bulk-geoapify-validate.js`
- `scripts/check-duplicates-initial.js`
- `scripts/migrations/001_geoapify_address_validation.sql`
- `features/PROJ-24-architektur.md`
- `features/PROJ-25-easybill-rabatte.md` (Spec, wartet auf "approved")

### Geänderte Dateien:
- `src/lib/easybill/partner-sync.ts` (Geoapify-Integration)
- `src/lib/actions/addresses.ts` (Auto-Revalidierung nach Update)
- `src/app/(app)/kunden/components/kunden-search.tsx` (Toggle)
- `src/app/(app)/kunden/page.tsx` (showInactive prop)
- `src/lib/actions/partners.ts` (getPartnersWithRevenue mit showInactive)
- `src/proxy.ts` (Webhook Auth-Ausnahme)
- `features/PROJ-24-easybill-partner-webhook-sync.md` (Status: Deployed)
- `features/INDEX.md` (PROJ-24 auf Deployed)

### Key Learnings:
- `supabase-db` Docker Container auf Hetzner — direkter DB-Zugriff via `docker exec`
- Altes Geoapify-System existierte schon! `geoapify_validation_status` etc.
- Spalten mussten migriert werden (alte → neue Namen)
- Service-Role-Key für Admin-Seiten (bypass RLS)
- `revalidatePath` nach Adress-Update notwendig für UI-Refresh

### Nächste Schritte:
- PROJ-24 Status: **Vollständig deployed** ✅
- PROJ-25 (Easybill Rabatte) — Spec wartet auf Jan Bernds "approved"
- Cronjob läuft automatisch, Webhook ist live
